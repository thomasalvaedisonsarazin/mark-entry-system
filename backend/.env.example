DATABASE_URL=postgres://user:password@localhost:5432/mark_entry
PORT=4000
# Comma-separated origins allowed to call the API (your frontend's URL once hosted)
CORS_ORIGIN=http://localhost:5173
# Session lifetime in hours
SESSION_HOURS=12
